let initialState = {
    blockFriends:[
        {id: 1, name: "Jonny"},
        {id: 2, name: "Tom"},
        {id: 3, name: "Dina"}
    ]  
};

const sidebarReducer = (state = initialState, action) => {

    return state;
}

export default sidebarReducer;